You are carrying the Shinobi specialist role (DevOps, Kubernetes, GitOps, containers).

Your working directory is /home/gerso/src/lab (a git repository).

TASK: Perform a bounded comparison of three Calibre container image options for a headless calibre-server deployment in our Kubernetes homelab. This is a SMALL focused investigation — do NOT repeat the broad Calibre research already completed.

Compare these three options:

1. rloomans/calibre-server (Docker Hub)
   - Check: https://hub.docker.com/r/rloomans/calibre-server
   - Is it maintained? Last update? Stars/pulls?
   - Does it run non-root? What UID/GID?
   - What ports does it expose?
   - Does it include any desktop/VNC/terminal services?
   - Can it be pinned by digest?
   - What is the Dockerfile base / provenance?

2. A minimal Ninjatronics-owned image built from official upstream Calibre
   - What would the Dockerfile look like?
   - Base image (e.g., ubuntu/debian/alpine + calibre package)?
   - How to install calibre-server without the GUI?
   - What would the maintenance burden be?
   - Could we pin by digest and reproduce builds?

3. lscr.io/linuxserver/calibre (LinuxServer)
   - Check: https://hub.docker.com/r/linuxserver/calibre
   - Does it include desktop/VNC? (We already know it does)
   - Can we expose ONLY the calibre-server port?
   - PUID/PGID support?
   - Maintenance quality?

For each option, report:
- Upstream provenance
- Maintenance status (last update, frequency)
- Attack surface (what services run, what's exposed)
- Non-root support (exact UID/GID)
- Reproducibility (digest pinning, build reproducibility)
- Headless-only operation (no GUI/VNC/terminal)
- Update/patching responsibility
- Recommended or not, and why

RECOMMEND ONE option with clear reasoning. If the decision presents a meaningful tradeoff requiring Gerso's approval, state that explicitly.

CONSTRAINTS:
- Read-only analysis. Do NOT create, modify, or delete any files.
- Do NOT create manifests, commits, or cluster changes.
- Ground findings in web research and actual Docker Hub/GitHub pages.
- Be concise. This is a bounded comparison, not a full research report.

Deliver your response between these markers:
<<<RESULT:LAB-CAL-IMG>>>
Place your comparison and recommendation here.
<<<END:LAB-CAL-IMG>>>
